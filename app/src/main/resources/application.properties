spring.application.name=green-release-demo
spring.profiles.active=default

# Server configuration
server.port=8080
server.servlet.context-path=/

# Logging configuration
logging.level.root=INFO
logging.level.com.greenrelease=DEBUG
